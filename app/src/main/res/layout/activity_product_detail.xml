<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@color/black">

    <ImageView
        android:id="@+id/ivDetailImage"
        android:layout_width="285dp"
        android:layout_height="285dp"
        android:layout_marginTop="25dp"
        android:layout_centerInParent="true"
        android:layout_alignParentTop="true"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_launcher_background" />

    <TextView
        android:id="@+id/tvDetailTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/ivDetailImage"
        android:layout_marginTop="16dp"
        android:textColor="@color/white"
        android:text="Title"
        android:layout_centerHorizontal="true"
        android:textSize="24sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tvProductArtist"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvDetailTitle"
        android:text="Artist"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:gravity="center" />

    <!-- LinearLayout to hold artist name and year side by side, positioned below the title -->

    <TextView
        android:id="@+id/tvDetailDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvProductArtist"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:text="Description"
        android:textColor="@color/white"
        android:textSize="16sp" />

</RelativeLayout>
